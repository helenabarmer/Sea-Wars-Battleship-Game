package com.company;

public enum SquareState {
    //Human human = new Human;

    if(){
        if(=={X} || =={*}){
        }
        else{

        }
    }
    else {
        if(=={}){

        }
        else if(=={X} || =={*}){

        }
        else{

        }
    }

}
